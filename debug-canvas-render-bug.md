[OPEN] Canvas debugging session: canvas-render-bug

## Symptoms
- 页面画布无法正常渲染、交互失效或显示异常。

## Initial Hypotheses
- 假设 1：画布初始化时机不对，实例创建发生在容器尺寸为 0 或 DOM 未完成挂载之前。
- 假设 2：渲染参数或缩放参数错误，导致实际绘制区域与可视区域不一致。
- 假设 3：事件绑定目标丢失或被覆盖，鼠标/触控事件没有正确绑定到当前画布实例。
- 假设 4：资源加载流程异常，字体、图片或依赖模块未就绪时提前触发渲染。
- 假设 5：响应式更新或页面切换时销毁/重建逻辑不完整，造成旧实例残留或新实例未生效。

## Status
- 已创建调试会话，等待代码定位与运行时证据。
